Context compaction is imminent. The session will continue after compaction. No action is usually needed.

Commands (optional):
  {MENU_DRIVER_PATH} {SESSION_NAME} snapshot              Take a pane snapshot before compaction
  {MENU_DRIVER_PATH} {SESSION_NAME} clear_then <command>  Clear and restart if context is critically high
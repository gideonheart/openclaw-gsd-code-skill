---
phase: 17-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOCS-06]

must_haves:
  truths:
    - "README.md config files table includes scripts/prompts/*.md entry with description of per-hook instruction templates"
    - "README.md shared libraries table shows 10 functions for lib/hook-utils.sh (not 9)"
    - "README.md mentions prompt template placeholder variables ({SESSION_NAME}, {MENU_DRIVER_PATH}, {SCRIPT_DIR})"
  artifacts:
    - path: "README.md"
      provides: "Updated admin documentation with prompt template files in config table"
      contains: "scripts/prompts/"
  key_links:
    - from: "README.md"
      to: "scripts/prompts/*.md"
      via: "config files table entry"
      pattern: "scripts/prompts/"
---

<objective>
Update README.md to include scripts/prompts/*.md in the config files table, update the shared libraries function count to 10, and add a brief note about the prompt template system.

Purpose: Ensure admin-facing documentation reflects the new per-hook prompt template files as configurable assets and the updated library function count.

Output: Updated README.md with prompt template file references and correct function count.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md config files table and shared libraries</name>
  <files>README.md</files>
  <action>
Make these specific changes to README.md:

1. **Config Files table** (under "### Config Files"): Add a new row after the existing three entries:
   - File: `scripts/prompts/*.md`
   - Description: Per-hook instruction templates (7 files). Each template contains trigger-specific [ACTION REQUIRED] content loaded by hooks via `load_hook_prompt()`. Placeholders: `{SESSION_NAME}`, `{MENU_DRIVER_PATH}`, `{SCRIPT_DIR}`. Editable without modifying hook scripts.

2. **Shared Libraries table** (under "### Shared Libraries"): Change the description for lib/hook-utils.sh from "9 functions" to "10 functions" to reflect the addition of load_hook_prompt().

3. **Menu-driver.sh entry** (in the Scripts table or wherever menu-driver.sh actions are listed): Verify the actions list includes `arrow_up`, `arrow_down`, `space` alongside existing actions. If the current README lists actions, add these three. The current list shows: `snapshot`, `enter`, `esc`, `clear_then <cmd>`, `choose <n>`, `type <text>`, `submit`. Add `arrow_up`, `arrow_down`, `space` to the SKILL.md actions list if not already present (README.md may reference SKILL.md for action details). If the README lists the actions inline, add the three new actions to the list.
  </action>
  <verify>
grep "scripts/prompts/" README.md should find the new config files table entry.
grep "10 functions" README.md should find the updated count.
grep "load_hook_prompt" README.md should find at least one reference.
  </verify>
  <done>README.md config files table includes scripts/prompts/*.md with description of per-hook instruction templates and placeholder variables, shared libraries table shows 10 functions, menu-driver actions are up to date</done>
</task>

</tasks>

<verification>
1. `grep "scripts/prompts/" README.md` — template files entry present in config files table
2. `grep "10 functions" README.md` — updated count in shared libraries table
3. `grep "load_hook_prompt" README.md` — at least one reference to the loading function
4. `grep "SESSION_NAME" README.md` — placeholder variables documented
</verification>

<success_criteria>
- README.md config files table has scripts/prompts/*.md entry with template description and placeholders
- README.md shared libraries table says 10 functions (not 9)
- README.md references load_hook_prompt() in the prompt template entry
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation/17-02-SUMMARY.md`
</output>
